---
import BaseLayout from '../layouts/BaseLayout.astro';
import Nav from '../components/Nav.astro';
import IntelligenceSection from '../components/IntelligenceSection.astro';
import Footer from '../components/Footer.astro';
import { ShoppingBag } from 'lucide-astro';
import { intelligenceData } from '../data/intelligence';

const shopifyData = intelligenceData.filter(item => item.category === 'Shopify');
---

<BaseLayout title="Shopify">
  <Nav />
  
  <main class="sub-page">
    <div class="container">
      <header class="mini-header">
        <nav class="breadcrumbs">
          <a href="/" class="bc-link">EASY ECOM</a>
          <span class="bc-sep">/</span>
          <a href="/shopify" class="bc-link active">SHOPIFY</a>
        </nav>
      </header>
    </div>

    <IntelligenceSection 
      id="shopify-content" 
      title="Shopify Intelligence" 
      items={shopifyData} 
      type="emerald" 
      icon={ShoppingBag} 
      showViewAll={false}
    />
  </main>

  <Footer />
</BaseLayout>

<style>
  .sub-page {
    padding-top: 8rem;
    min-height: 100vh;
  }

  .mini-header {
    margin-bottom: 3rem;
    border-bottom: 1px solid var(--zinc-200);
    padding-bottom: 1rem;
  }

  .breadcrumbs {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.65rem;
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: 0.3em;
  }

  .bc-link {
    color: var(--zinc-500);
    transition: var(--transition-fast);
  }

  .bc-link:hover {
    color: var(--black);
  }

  .bc-link.active {
    color: var(--black);
  }

  .bc-sep {
    color: var(--zinc-300);
  }

  .emerald { color: #10b981; }

  @media (max-width: 768px) {
    .sub-page { padding-top: 6rem; }
  }
</style>
